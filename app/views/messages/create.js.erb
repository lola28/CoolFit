function refreshForm(innerHTML) {
  const newMessageForm = document.getElementById('new_message');
  newMessageForm.innerHTML = innerHTML;
}

function addMessage(messageHTML) {
  const messages = document.getElementById('messages');
  messages.insertAdjacentHTML('beforeend', messageHTML);
}

function addMessages() {
  const messages = document.getElementById('messages');
  messages.innerHTML = '<%= j render "messages/show", message: @message %>'
}

<% if @message.errors.any? %>
  refreshForm('<%= j render "messages/form", activity: @activity, message: @message %>');
<% else %>
  addMessages();
  refreshForm('<%= j render "messages/form", activity: @activity, message: Message.new %>');
<% end %>
