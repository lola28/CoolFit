if (document.querySelector("[data-interests-id='<%= @activity.id %>']")) {
  document.querySelector("[data-interests-id='<%= @activity.id %>']").remove();
} else {
  document.querySelector("[data-activity-id='<%= @activity.id %>'] > a").innerHTML = "♡";
  document.querySelector("[data-activity-id='<%= @activity.id %>'] > a").setAttribute('data-method', 'post');
};

