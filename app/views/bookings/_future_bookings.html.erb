<div class="card-dashboard d-flex justify-content-between" data-future-bookings-id="<%= activity.id %>">
  <%= render 'shared/card_index', activity: activity %>

  <div class="card-btns">
    <%= link_to "Unregister", activity_booking_path(activity.id, booking), method: :delete, remote: true, class: 'btn admin-btn' %>
  </div>

</div>

