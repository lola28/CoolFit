<%= simple_form_for booking, html: { class: "booking_form" },url: activity_booking_path(booking.activity), method: :patch do |f| %>
  <%= f.input :rating, collection: (1..5).to_a, label: "Rate your experience", id: "booking_rating" %>
<% end %>
