<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <%= render 'shared/head' %>
  </head>

  <body>
    <%= render 'shared/flashes' %>

    <%= render 'shared/navbar' unless in_authentication_page? %>

    <div class="scroll-container <%= in_authentication_page? ? 'full-height' : '' %>">
      <div class="<%= in_authentication_page? ? 'devise-background' : 'container mt-4 page-wrapper' %>">
        <%= yield %>
      </div>

      <%= render 'shared/footer' %>

      <p class="alert"><%= alert %></p>
    </div>


    <%= javascript_include_tag 'application' %>
    <%= javascript_pack_tag 'application' %>
  </body>
  <script src="https://media-library.cloudinary.com/global/all.js"></script>
</html>
